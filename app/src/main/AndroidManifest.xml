<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ling.ours">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_yl_heart"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_yl_heart"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".MainViewActivity" />
        <activity android:name=".OurActivity" />
        <activity android:name=".CareActivity" />
        <activity android:name=".GoActivity" />
        <activity android:name=".PlayActivity" />
        <activity android:name=".RemberActivity" />
        <activity android:name=".WriteActivity" />
        <activity android:name=".EatActivity" />

        <service android:name=".MusicService">
            <intent-filter>
                <action android:name="com.example.ling.ours.MusicService" />
            </intent-filter>
        </service>


    </application>

</manifest>